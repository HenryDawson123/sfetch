/*using #pragma once here, idk whether i should use a different header guard instead but it is what it is*/
#pragma once
/*This is basically all of the includes/headers for the main.cpp, they are in a seperate file for simplicity and easier navigation of main.cpp*/

#include <fstream>
#include <sstream>
#include <string>
#include <algorithm>
#include <sys/utsname.h>
#include <sys/sysinfo.h>
#include <chrono>
